; vim: set expandtab:smarttab
@           1D  IN SOA  ns0.wikimedia.org.  hostmaster.wikimedia.org.   (
                    {{ serial }}        ; serial
                    12H     ; refresh
                    2H      ; retry
                    2W      ; expiry
                    1H      ; negative cache TTL
                    )

; Name servers

            1D  IN NS   ns0.wikimedia.org.
            1D  IN NS   ns1.wikimedia.org.
            1D  IN NS   ns2.wikimedia.org.

; Mail exchangers

; SPF txt and rr records (no email)
            600 IN TXT  "v=spf1 -all"

; Canonical names
            600 IN DYNA geoip!text-addrs

; CAA records
@           600 IN CAA 0 issue "globalsign.com"
@           600 IN CAA 0 issue "digicert.com"
@           600 IN CAA 0 iodef "mailto:dns-admin.org"

; Servers (alphabetic order)
gerrit      600 IN DYNA geoip!text-addrs
phab        600 IN DYNA geoip!text-addrs

